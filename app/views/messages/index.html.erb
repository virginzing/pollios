<div class="well">
  <a href="<%= new_message_path %>" class="btn btn-primary btn-lg">New Message</a>
</div>

<section id="widget-grid" class="">
  <div class="jarviswidget" id="wid-list-gift-log" data-widget-editbutton="false" data-widget-colorbutton="false" data-widget-deletebutton="false" data-widget-fullscreenbutton="false">
    <header>
      <span class="widget-icon"> <i class="fa fa-gift"></i> </span>
      <h2><strong>List Message Log</strong></h2>          
    </header>

    <!-- widget div-->
    <div>
      
      <!-- widget content -->
        <div class="widget-body no-padding">

          <table id="message_logs" class="table table-striped table-bordered table-hover" width="100%">
            <thead>
              <tr>
                <th>#</th>
                <th>Admin Id</th>
                <th>Message</th>
                <th>List Member</th>
              </tr>
            </thead>
            <tbody>
              <% @message_logs.each do |message_log| %>
                <tr>
                  <td><%= message_log.id %></td>
                  <td><%= message_log.admin_id %></td>
                  <td><%= message_log.message %></td>
                  <td><%= message_log.list_member %></td>
                </tr>
              <% end %>
            </tbody>
          </table>

        </div>
      <!-- end widget content -->
      
    </div>
    <!-- end widget div -->

  </div>
</section>