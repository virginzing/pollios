<div class="well">
<div class="row">
  <div class="col-md-12">
    <h1 class="txt-color-blueDark page-well-header">
        <%= image_tag(@group.photo_group.url(:thumbnail), size: '50x50') %> <%= @group.name %> Group
    </h1>
  </div>
</div>
</div>


<div class="well">
  <h4 class="alert-heading">Please Note!</h4>
  <p>
    Lorem ipsum dolor sit amet, consectetur adipiscing elit. Maecenas magna lectus, eleifend sed augue a, commodo auctor turpis. Praesent ornare mi est, ac rutrum felis pharetra nec. Morbi aliquam turpis ut sagittis gravida. Quisque ut lacus eget justo hendrerit dignissim eget et nisl. Morbi quis augue lectus. Praesent placerat ultricies dolor, ut fringilla magna.<strong> Pollios</strong>Pellentesque habitant morbi tristique senectus et netus et malesuada fames ac turpis egestas.
  </p>
  <br>
  <a href="<%= freeform_poll_url %>" class="icon-btn">
    <i class="fa fa-edit"></i>
    <div>Poll</div>
  </a>

  <a href="<%= company_add_member_url %>" class="icon-btn">
    <i class="fa fa-plus"></i>
    <div>User</div>
  </a>

  <a href="<%= company_edit_group_path %>" class="icon-btn">
    <i class="fa fa-cog"></i>
    <div>Setting</div>
  </a>

</div>

<section id="widget-grid" class="">

  <!-- row -->
  <div class="row">

    <!-- NEW WIDGET START -->
    <article class="col-md-8">

      <!-- Widget ID (each widget will need unique ID)-->
      <div class="jarviswidget jarviswidget-color-blueDark" id="wid-group-<%= params[:group_id] %>-company-polls" data-widget-colorbutton="false" data-widget-editbutton="false">
        <header>
          <span class="widget-icon"> <i class="fa fa-edit"></i> </span>
          <h2><strong>List Poll</strong></h2>
        </header>

        <!-- widget div-->
        <div>

          <!-- widget content -->
          <div class="widget-body no-padding">
            <table id="list_default" class="table table-striped table-bordered table-hover" width="100%">
              <thead>
                <tr>
                  <th width="50">#</th>
                  <th width="50"></th>
                  <th width="150">Title</th>
                  <th>Vote All</th>
                  <th>View All</th>
                  <th width="100">Created At</th>
                  <th>Qrcode</th>
                  <!-- <th>Re-Generate</th> -->
                  <th>Action</th>
                </tr>
              </thead>
              <tbody>
                <% @polls.each do |poll| %>
                  <tr>
                    <td><%= poll.id %></td>
                    <td><img src="<%= poll.member.avatar.url(:thumbnail_small) %>" alt="user" class="img img-circle img-responsive center-block" data-toggle="tooltip" data-placement="top" title="<%= poll.member.fullname %>"></td>
                    <td><%= link_to poll.title, company_group_poll_detail_path(@group, poll) %></td>
                    <td><%= poll.vote_all %></td>
                    <td><%= poll.view_all %></td>
                    <td><%= poll.created_at.strftime("%B %d, %Y") %></td>
                    <td><button data-toggle="modal" class="btn btn-primary" data-target="#myModal" onclick="showIFrame('/poll/<%= poll.id %>/qrcode.png', '<%= poll.title %>')">Show</button></td>
<!--                     <td>
                      <%= form_tag '/poll/new_generate_qrcode' do %>
                        <%= hidden_field_tag 'id', poll.id %>
                        <%= submit_tag 'Re new', class: 'btn btn-primary' %>
                      <% end %>
                    </td> -->
                    <td><%= link_to 'Delete', poll_path(poll), method: :delete, data: { confirm: 'Are you sure?' }, class: 'btn btn-default btn-danger' %></td>
                  </tr>
                <% end %>
              </tbody>
            </table>
          </div>
          <!-- end widget content -->

        </div>
        <!-- end widget div -->

      </div>
      <!-- end widget -->

      <div class="jarviswidget jarviswidget-color-blueDark" id="wid-group-<%= params[:group_id] %>-company-members" data-widget-editbutton="false" data-widget-colorbutton="false">
        <header>
          <span class="widget-icon"> <i class="fa fa-group"></i> </span>
          <h2><strong>List Member</strong></h2>          
        </header>

          <!-- widget div-->
        <div>
          
          <!-- widget content -->
            <div class="widget-body no-padding">

              <table id="list_members" class="table table-striped table-bordered table-hover" width="100%">
                <thead>
                  <tr>
                    <th width="50">#</th>
                    <th width="50">Avatar</th>
                    <th>Name</th>
                    <th>Email</th>
                    <th>Status</th>
                    <th>Joined at</th>
                    <th>Action</th>
                  </tr>
                </thead>
                <tbody>
                  <% @members.each do |member| %>
                    <tr>
                      <td><%= member.id %></td>
                      <td><img src="<%= member.avatar.url(:thumbnail_small) %>" alt="user" class="img img-circle img-responsive center-block" data-toggle="tooltip" data-placement="top" title="<%= member.fullname %>"></td>
                      <td><%= member.get_name %></td>
                      <td><%= member.email %></td>
                      <td><a href="#" class="admin_status" data-type="select" data-pk="<%= member.id %>" data-url="/group/<%= params[:group_id] %>/group_update.json" data-original-title="Select Status" data-value="<%= member.admin ? 2 : 1 %>"><%= member.is_admin_group(member.admin) %></a></td>
                      <td><span data-livestamp="<%= member.joined_at.to_i %>"></span></td>
                      <td><a class="btn btn-default btn-danger" data-confirm="Are you sure?" data-method="delete" href="<%= remove_member_group_path(member_id: member.id, group_id: params[:group_id]) %>" rel="nofollow">Delete</a></td>
                    </tr>
                  <% end %>
                </tbody>
              </table>

            </div>
        <!-- end widget content -->
          
        </div>
        <!-- end widget div -->
      </div>

      <div class="jarviswidget jarviswidget-color-blueDark" id="wid-group-<%= params[:group_id] %>-list_surveyor" data-widget-editbutton="false" data-widget-colorbutton="false">
        <header>
          <span class="widget-icon"> <i class="fa fa-group"></i> </span>
          <h2><strong>List Surveyor</strong></h2>          
        </header>

          <!-- widget div-->
        <div>
          
          <!-- widget content -->
            <div class="widget-body no-padding">

              <table id="list_surveyor" class="table table-striped table-bordered table-hover" width="100%">
                <thead>
                  <tr>
                    <th width="50">#</th>
                    <th width="50">Avatar</th>
                    <th>Name</th>
                    <th>Email</th>
                    <th>Action</th>
                  </tr>
                </thead>
                <tbody>
                  <% @list_surveyor.each do |member| %>
                    <tr>
                      <td><%= member.id %></td>
                      <td><img src="<%= member.avatar.url(:thumbnail_small) %>" alt="user" class="img img-circle img-responsive center-block" data-toggle="tooltip" data-placement="top" title="<%= member.fullname %>"></td>
                      <td><%= member.get_name %></td>
                      <td><%= member.email %></td>
                      <td><a class="btn btn-default btn-danger" data-confirm="Are you sure?" data-method="delete" href="<%= company_remove_surveyor_path(@group.id, member.id) %>" rel="nofollow">Remove Surveyor</a></td>
                    </tr>
                  <% end %>
                </tbody>
              </table>

            </div>
        <!-- end widget content -->
          
        </div>
        <!-- end widget div -->
      </div>

    </article>
    <!-- WIDGET END -->

    <!-- NEW WIDGET START -->
    <article class="col-md-4">

      <!-- Widget ID (each widget will need unique ID)-->
      <div class="jarviswidget jarviswidget-color-blueDark" id="wid-group-<%= params[:group_id] %>-company-activity" data-widget-colorbutton="false" data-widget-editbutton="false">
        <header>
          <span class="widget-icon"> <i class="fa fa-globe"></i> </span>
          <h2>Activity Feed</h2>
        </header>

        <!-- widget div-->
        <div>

          <!-- widget content -->
          <div class="widget-body">
            <div>
                <div class="feed-activity-list">
                  <% 5.times do %>
                    <div class="feed-element">
                        <a href="profile.html" class="pull-left">
                            <img alt="image" class="img-circle" src="/img/profile.jpg">
                        </a>
                        <div class="media-body ">
                            <small class="pull-right">5m ago</small>
                            <strong>Misa</strong> vote poll <strong>ทำอะไรดีเน้อ ?</strong>. <br>
                            <small class="text-muted">Today 5:60 pm - 12.06.2014</small>
                        </div>
                    </div>
                    <div class="feed-element">
                        <a href="profile.html" class="pull-left">
                            <img alt="image" class="img-circle" src="/img/profile.jpg">
                        </a>
                        <div class="media-body ">
                            <small class="pull-right">10m ago</small>
                            <strong>Misa</strong> view poll <strong>What's your favorite color</strong>. <br>
                            <small class="text-muted">Today 5:60 pm - 12.06.2014</small>
                        </div>
                    </div>
                  <% end %>
                </div>
                <button class="btn btn-primary btn-block m-t">Show More</button>
            </div>
          </div>
          <!-- end widget content -->

        </div>
        <!-- end widget div -->

      </div>
      <!-- end widget -->

    </article>
    <!-- WIDGET END -->

  </div>

  <!-- end row -->

  <!-- row -->
</section>

 <script type="text/javascript">
  $('[rel=tooltip]').tooltip();
  function load_qrcode(poll_id) {

    BootstrapDialog.show({
          title: 'Qrcode',
          message: function(dialog) {
                var $message = $('<div></div>');
                var pageToLoad = dialog.getData('pageToLoad');
                $message.load(pageToLoad);
        
                return $message;
            },
            data: {
                'pageToLoad': '/poll/' + poll_id + '/qrcode'
            }
    });
  }
</script>

 <script type="text/javascript">
 function showIFrame(url, title) {
    var iframebox = document.getElementById('iframebox');
    iframebox.src = url;
    var desc = document.getElementById('title');
    desc.innerHTML = title;
 }
 </script>

<script type="text/javascript">
  $( document ).ready(function() {
    $('.admin_status').editable({
      showbuttons: false,
      source: [
              {value: 1, text: 'Member'},
              {value: 2, text: 'Admin'}
      ],
      value: function(value) {
        $(this).text(value);
      },
      error: function(response, newValue) {
          if(response.status === 500) {
              return 'Service unavailable. Please try later.';
          } else {
              return response.responseText;
          }
      }
    });

  });
</script>

<!-- Modal -->
<div class="modal fade" id="myModal" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true">
  <div class="modal-dialog">
    <div class="modal-content">
      <div class="modal-header">
        <button type="button" class="close" data-dismiss="modal"><span aria-hidden="true">&times;</span><span class="sr-only">Close</span></button>
        <h4 class="modal-title" id="myModalLabel">QR Code</h4>
      </div>
      <div class="modal-body">
        <div style="padding-left: 210px;">
          <iframe src="" id="iframebox"></iframe>
        </div>
        <img style="position: absolute; top: 20px; left: 230px; opacity:0.3;" src="/img/pollios3.png" />
        <h2 id="title" class="text-center"></h2>
      </div>
    </div>
  </div>
</div>