$.confirm = <%= raw @confirm || false %>;

if ($.confirm) {
  handleConfirmSuccess();
} else {

};

function handleConfirmSuccess() {
  window.location.href = "<%= company_poll_with_campaign_url(poll) %>";
}